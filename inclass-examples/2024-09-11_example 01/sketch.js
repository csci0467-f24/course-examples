function setup() {
  createCanvas(600, 600);
  // noLoop();
  background(200,200,255);
}

function draw() {
  // background(220);

  // Draw a circle
  fill("red");
  ellipse(mouseX, mouseY, 50, 50);
 
}
